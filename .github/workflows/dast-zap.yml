name: Production ZAP baseline

on:
  workflow_run:
    workflows: ["VDS production deploy"]
    types: [completed]
  schedule:
    - cron: "29 5 * * 2"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  zap-baseline:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: ZAP passive baseline scan
        uses: zaproxy/action-baseline@de8ad967d3548d44ef623df22cf95c3b0baf8b25 # v0.15.0
        with:
          target: https://netarandevu.com
          fail_action: true
          allow_issue_writing: false
          cmd_options: '-j'
